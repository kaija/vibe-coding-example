# Database Configuration
# For local development
DATABASE_URL=postgresql://username:password@localhost:5432/note_api
# For Docker development
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/note_api

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Server Configuration
ROCKET_PORT=8000
# For local development
ROCKET_ADDRESS=127.0.0.1
# For Docker development (uncomment the line below)
# ROCKET_ADDRESS=0.0.0.0

# Logging Configuration
RUST_LOG=info

# Docker-specific PostgreSQL Configuration
# These are used in docker-compose.yml
POSTGRES_DB=note_api
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres